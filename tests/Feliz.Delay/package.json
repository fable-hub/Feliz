{
  "private": true,
  "name": "feliz-delay-tests",
  "version": "0.0.0",
  "scripts": {
    "transpile": "dotnet fable -o ./fableoutput --exclude Feliz.CompilerPlugins --noCache -e .jsx",
    "test": "npm run transpile && vitest --run --config ../vitest.config.ts",
    "test:watch": "npm run transpile -- --watch --run vitest --config ../vitest.config.ts"
  }
}
