{
  "private": true,
  "name": "feliz-tests",
  "version": "0.0.0",
  "scripts": {
    "transpile": "dotnet tool restore && dotnet fable -o ./fableoutput --exclude Feliz.CompilerPlugins --noCache",
    "test": "npm run transpile && vitest --run",
    "test:watch": "npm run transpile -- --watch --run vitest"
  }
}
